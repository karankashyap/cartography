# Ollama (default provider)
OLLAMA_MODEL=llama3.2

# LM Studio (optional — set LM_STUDIO_URL to your host LM Studio server)
# LM Studio listens on port 1234 by default; host.docker.internal reaches the host from containers
LM_STUDIO_URL=http://host.docker.internal:1234
LM_STUDIO_MODEL=gemma-4-27b
